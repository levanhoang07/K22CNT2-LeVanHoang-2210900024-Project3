<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Kích hoạt annotation-driven (quan trọng cho @RequestMapping, @GetMapping, v.v.) -->
    <mvc:annotation-driven/>
    
    <!-- Quét các package để tìm Controller và Component -->
    <context:component-scan base-package="com.lvhspringmvc.controllers"/>

    <!-- Cấu hình ViewResolver cho JSP -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>
    
    <!-- Cấu hình DataSource (Kết nối MySQL) -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
        <property name="url" value="jdbc:mysql://localhost:3306/K22CNT3_LeVanHoang_2210900024_Project3?useSSL=false&amp;serverTimezone=UTC"/>
        <property name="username" value="root"/>
        <property name="password" value="Hoang@123"/>
    </bean>

    <!-- Cấu hình JdbcTemplate -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    
    <!-- Cấu hình Transaction Manager -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    
    <!-- Bật hỗ trợ annotation @Transactional -->
    <context:annotation-config/>

    <!-- Định nghĩa DAO -->
    <bean id="QuantriDAO" class="com.lvhspringmvc.dao.QuantriDAO">
        <property name="jdbcTemplate" ref="jdbcTemplate"/>
    </bean>

</beans>